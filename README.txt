# Raymond's Website

Open `index.html` in a browser to run the website.

This is the standalone version of the website, so it no longer needs the Python
`127.0.0.1:8080` local server.

To publish it, upload `index.html` to a static web host such as Vercel or Netlify.
